# Local dev (npm run dev) — dùng .env.development hoặc copy:
# VITE_BACKEND_URL=http://localhost:8080/api/v1

# Production (npm run build / Render) — đã có trong .env.production:
VITE_BACKEND_URL=https://be-2-emw3.onrender.com/api/v1
