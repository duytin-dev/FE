# Backend API
VITE_BACKEND_URL=http://localhost:8080/api/v1

# Cloudinary — chỉ cloud name (public). KHÔNG đặt API_SECRET / CLOUDINARY_URL vào FE — sẽ lộ trên browser.
VITE_CLOUDINARY_CLOUD_NAME=diodfucny

# Upload từ browser (tùy chọn, sau khi tạo Unsigned preset trên Cloudinary):
# VITE_CLOUDINARY_UPLOAD_PRESET=your_preset_name
